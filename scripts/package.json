{
  "name": "granada-vibes-scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "cheerio": "^1.2.0",
    "tsx": "^4.0.0"
  },
  "scripts": {
    "fetch": "node --env-file=../.env --import tsx fetch-events.ts",
    "clean": "node --env-file=../.env --import tsx clean-events.ts",
    "sync": "node --env-file=../.env --import tsx sync-events.ts"
  }
}
