---
import RouteDetailPage from '@components/pages/RouteDetailPage.astro';
import { getRouteStaticPaths } from '@utils/static-paths';

export const getStaticPaths = getRouteStaticPaths;

const { route } = Astro.props;
---

<RouteDetailPage locale="es" route={route} />
