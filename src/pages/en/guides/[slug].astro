---
import BlogDetailPage from '@components/pages/BlogDetailPage.astro';
import { getBlogStaticPaths } from '@utils/static-paths';

export const getStaticPaths = getBlogStaticPaths;

const { post } = Astro.props;
---

<BlogDetailPage locale="en" post={post} />
