---
import EventDetailPage from '@components/pages/EventDetailPage.astro';
import { getEventStaticPaths } from '@utils/static-paths';

export const getStaticPaths = getEventStaticPaths;

const { event } = Astro.props;
---

<EventDetailPage locale="en" event={event} />
